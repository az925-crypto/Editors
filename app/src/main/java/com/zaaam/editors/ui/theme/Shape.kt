package com.zaaam.editors.ui.theme

import androidx.compose.ui.graphics.CornerSize
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

object RetroShapes {
    val Shell = 22.dp
    val HeroCard = 18.dp
    val Card = 16.dp
    val SearchBtn = 14.dp
    val Dialog = 20.dp
    val TabTop = 12.dp
    val Chip = 10.dp
    val Pill = 999.dp
    val Stencil = 10.dp
    val NavItem = 12.dp

    val ShellCorner = CornerSize(Shell)
    val HeroCardCorner = CornerSize(HeroCard)
    val CardCorner = CornerSize(Card)
    val SearchBtnCorner = CornerSize(SearchBtn)
    val DialogCorner = CornerSize(Dialog)
    val TabTopCorner = CornerSize(TabTop)
    val ChipCorner = CornerSize(Chip)
    val PillCorner = CornerSize(Pill)
    val StencilCorner = CornerSize(Stencil)
    val NavItemCorner = CornerSize(NavItem)
}